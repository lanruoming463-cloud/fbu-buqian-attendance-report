# 2026-09-04 工作日志

- 2026-09-04 09:16 在「补签流程_补签统计」报表中新增「考勤确认及时性（劳务工）」看板板块。
  - 数据源：D:/Documents/Downloads/考勤确认及时性劳务工（07.01-07.31）(1).xlsx（本期 2026-07）、考勤确认及时性劳务工（06.01-06.30）(1).xlsx（上期 2026-06）的「考勤确认及时性」sheet。
  - 脚本 gen_buqian_stats.py 新增：_read_attend_sheet() / _agg_attend() / build_attend_payload()；按 REGION_TO_MAJOR 将 17 个区域归入三大区（美洲区/欧洲区/亚太区，加拿大区归美洲区），计算 大区/区域/仓 三级的 应打卡次数、应补签数、补签率、考勤应确认数、考勤确认率 及本期vs上期对比（Δpp）。
  - 看板交互：顶部新增「考勤确认及时性」tab（独立 view-panel）；KPI 卡片（应打卡次数/应补签数/补签率/考勤确认率）+ 大区×区域×仓对比表（点击区域展开各仓）+ 对比视图/本期/上期切换。
  - 修复 bug：build_attend_payload 中聚合结果 key 应为 region/wh（非源表中文 区域/仓库），修正 w['仓库']→w['wh']、r['区域']→r['region'] 共 3 处。
  - 校验：本期总 应打卡次数 365,304 / 应补签数 66,335 / 补签率 18.2% / 考勤确认率 99.98%；3 大区、17 区域、103 仓。
  - 重新生成 HTML（9.7MB）/Excel 并部署：commit d8d264a，push 后远程 sprint/default = d8d264a；release 7681484482411351238 finished。online_url 不变 https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf
  - 部署踩坑：lark-cli 路径变更为 C:/Users/zt25337/.workbuddy/binaries/node/cli-connector-packages/lark-cli(.cmd)（原 versions/22.22.2/ 下已不存在，现为 22.22.2-2）；which lark-cli 可用。后续部署用此路径。

- 2026-09-04 09:33 将考勤确认及时性（劳务工）看板数据同步写入 Excel（此前 Excel 仅含正式工 sheet）。
  - gen_buqian_stats.py 在 attend_payload 构建后、向已生成的 xlsx 追加三个 sheet：考勤确认及时性_大区（4大区+合计）、考勤确认及时性_区域（17区域）、考勤确认及时性_仓（103仓）；列含 应打卡次数/应补签数/补签率/考勤确认率 的 本期、上期、Δpp。
  - 踩坑修正：attend DataFrame 不能直接 to_excel(openpyxl.Workbook)，改用 pd.ExcelWriter(OUT_XLSX, engine='openpyxl', mode='a', if_sheet_exists='replace') 追加。
  - 同步在 说明 sheet 增加考勤看板口径说明行。
  - 重新生成确认：Excel 10 个 sheet 齐全；HTML 本次无代码改动，飞书发布 d8d264a 仍有效，无需重发。

- 2026-09-04 11:10 考勤看板 v2 重做：改为「可选具体日期 + 逐层下钻」结构（对齐劳务工补签看板），并完成部署。
  - 数据源改为源文件的「劳务工-考勤表」sheet（逐人逐日，约 40 万行），新增 _read_attend_detail() / _agg_attend_day() / build_attend_payload_v2()。
    - 列索引：2=姓名,3=工号,4=工种,6=区域,7=仓库,8=组,10=考勤日期,44=应打卡数,45=实际打卡数,46=应补签数,47=实际补签数,48=应确认数,49=实际确认数,50=漏打卡数
    - 应补签数按 (工号, 考勤日期) 上限截断为 2（上下班两次打卡）
    - 指标 7 个：ydk/sjk/ybq/sjq/yqr/sjqr/ldk
  - 前端：按天/按月切换 + 日期选择器（date/month input）+ 前后翻页 + 区域筛选下拉 + 5 段表（大区汇总/区域明细/仓明细/组明细/Top员工），逐层点击下钻，组行可展开员工（每格上限 50 行，empCount 存真实人数）。
  - Excel 仍用 v1（考勤确认及时性 sheet 的汇总口径），HTML 用 v2，二者互不干扰。
  - **关键 bug（必记）**：attend_block 是 Python 普通三引号字符串，在「JS 单引号字符串内的内联 onclick」里写 \' 会被 Python 吞掉反斜杠 → 生成 'xxx(\'month\')' 变 'xxx('month')' → JS 语法错误。
    - 后果：发布流程报 finished 且 online_url 正常，但整块 JS 崩、看板白屏。**release status=finished 不等于页面可用**。
    - 修法：不要在 JS 字符串里拼内联 onclick，改用 document.createElement + addEventListener（ddItem(label, fn) 辅助函数），彻底规避引号转义。
    - 另修：stepAttend() 用了全局 days（正式工数据），改为 attendPayload.days 自包含。
  - **验证方法（可复用）**：无头 Chrome 注入探针 + 本地 HTTP 信标回传结果。
    - 不能只靠 --dump-dom：16MB 页面 dump 会丢尾部脚本，探针 div 拿不到。
    - 可靠方案：起一个 python http.server 抓 GET 参数 → 页面末尾注入脚本，用 `new Image().src='http://127.0.0.1:PORT/?d='+encodeURIComponent(JSON.stringify(report))` 回传（图片信标无 CORS 问题）。
    - head 里挂 window.onerror 收集运行时错误；chrome --headless=new --virtual-time-budget=20000 --dump-dom 触发。
  - 验证结果：errs=[]（零错误）；下拉 3 项/区域下拉 22 项；选 7-15 → 15,500 应打卡/2,682 应补签/17.3%；翻页 ±1 正常；大区下钻展开 9 个区域子行；区域筛选 160→21 行可还原。
  - 勾稽：7月 应打卡 186,706+231,720+6,784+3,164=428,374、应补签 10,506+65,853+126+289=76,774（17.9%）；6月 366,450/60,904(16.6%)；两月合计 794,824/137,678，与 Python 端全期总计一致；59/59 天大区级勾稽通过。
  - 注意：数据中存在「其他」大区（应打卡 3,164 / 应补签 289），即未能归入三大区的记录，非 bug 但可后续排查映射。
  - HTML 涨到 16.67MB（原 9.7MB），实测飞书妙搭可正常发布（24MB 才失败，9.8MB 的担心是多余的）。
  - 部署：commit 23052f4 → push（URL 内嵌凭证、git -c credential.helper= 直推）→ 用 python 字节替换修 .git/packed-refs 的 sprint/default 引用（git 2.55 写 packed-refs 静默失败的已知 BUG）→ release 7681510533755309266 finished，online_url https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf

- 2026-09-04 11:35 修复考勤看板大区映射遗漏（自己发现并修掉的数据 bug）。
  - 现象：看板出现「其他」大区（应打卡 3,164 / 应补签 289，占 0.4%）。
  - 根因：REGION_TO_MAJOR 只有 17 个区域，而「劳务工-考勤表」sheet 实际有 21 个区域，漏了 4 个：
    西雅图区→美洲区、诺福克区→美洲区、意大利区→欧洲区、韩国区→亚太区。
  - 修复：补全 4 个，并预防性加入主配置里已有的 印第安纳区/墨西哥区（均属美洲区）。
  - **新增防线**：加全局 `_UNMAPPED_REGIONS` 计数，构建结束时打印未命中映射的区域及行数，避免以后新增区域被静默归入「其他」。
  - 修复后：21 组 (major, region)，「其他」彻底消失；美洲区下钻区域 9→10 个；大区求和 = 全期总计（ydk 794,824 / ybq 137,678 / yqr 397,412）完全勾稽。
  - 残留提示：告警仍报 `'区域' 2 行`，是 v1（考勤确认及时性 sheet）读取时把表头文字当数据，2 行无有效数值、未污染 Excel 输出（区域 17 行、仓 103 行，均无「其他」）。可后续在 v1 里跳过表头行。
  - 注意：Excel 用 v1 口径（源 sheet 只有 17 区域），HTML 用 v2 口径（21 区域），两者本就覆盖范围不同，不是 bug。
  - 重新生成 + 部署：commit a094976 → release 7681518337066732786 finished；再次渲染验证 errs=[]、大区只剩 3 个、日期/翻页/下钻正常。

