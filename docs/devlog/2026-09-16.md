# 2026-09-16 工作日志

## 补签流程报表 — 三视图控件统一 + 妙搭重新发布

- 任务：用户要求「正式工/劳务工 考勤确认及时率」控件与各自「补签率」控件保持一致（此前完全不统一）。
- 改动（gen_buqian_stats.py）：三块视图（补签率 viewFormal / 正式工考勤确认及时率 viewGen / 劳务工考勤确认及时率 viewAttend）统一为同一套控件栏：`.nav` 容器内 [按天/按周/按月 pill] + ‹ + 日期 picker(▾) + › + 区域按钮(▾)，区域选择器统一为网格式面板（大区列 + 区域项，点击筛选）。CSS 选择器一并覆盖 #btnPicker/#genBtnPicker/#attBtnPicker 与 #btnRegion/#genBtnRegion/#attBtnRegion。劳务工区域下拉新增 pickAttMajor/pickAttRegion，attendRegionFilter 支持 大区 或 大区|区域 两种粒度。同步把看板名「考勤确认及时性」→「考勤确认及时率」。
- 校验（headless Chrome 探针）：三视图控件结构完全一致（各 6 个 nav 按钮 [按天,按周,按月,‹,2026年7月,›] + picker + 区域按钮，文案一致）；**零 JS 报错**；劳务工大区 23 行（3 大区 + 20 区域）齐全；劳务工区域网格实际填充正常（探针计数误用 attRpGrid 被 innerHTML 替换导致显示 0，非真实 bug）。
- 重新生成 补签流程_补签统计.html（gen_run16，约 18MB，EXIT=0）。
- 妙搭发布（app_17cg3t4mvjm）：commit 92be8d4 → push **d73c579..92be8d4**（fast-forward）→ +release-create → 轮询 finished（release_id=7685952575233051871，error_logs=[]）。
- online_url（不变）：https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf（创意模式需飞书登录）。

## 关键坑（务必记到 user MEMORY.md）：妙搭 git 部署 path 轮换 + 凭证绑定
- +git-credential-list 现在返回**新 path**：`https://miaoda-git.feishu.cn/apaas4.0/-/t_LLyIj779/code_u93l2C1EOhLX.git`，但 `git-credential-helper get` 对新 path（带/不带尾斜杠）**均返回空**；用旧 token 推新 path 报 `token is not authorized for this repository`(403)。
- 旧 path `https://miaoda-git.feishu.cn/apaas4.0/-/t_vkCL6f1I/code_p6iBZnfLI6KD.git` 经 `ls-remote` 验证**仍可用、token 已授权**（返回 sprint/default tip d73c579…）。
- 结论：**部署仍走旧 path + 旧 token**（与上次成功发布一致），不要被新 path 误导。凭证按仓库 path 绑定，非 app 级。
