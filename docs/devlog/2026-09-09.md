# 2026-09-09 工作日志

## 考勤报表重构（补签流程_补签统计）— 已完成并发布

- 需求：正式工考勤确认及时性 + 正式工/劳务工均分三板块（考勤准确率 / 考勤确认及时性 / 补签情况）。
- 口径（用户确认）：及时确认率分母=全部考勤；准确率=正常÷全部考勤；正式工非区域部门（HRBP部/财务部等）按区域归入三大区。
- 未及时确认规则：考勤状态=异常 且 异常备注/备注 不含"迟到/早退"（=缺卡未确认），正式工=1845 人。
- 关键数据：正式工 考勤总数98055 / 正常84132 / 异常13923 / 未及时确认1845 / 准确率85.8% / 及时确认率98.1%。
- 本次修复（gen_buqian_stats.py）：
  1. `_formal_major` 映射修正（欧洲区财务部→欧洲区，原误归"其他"）。
  2. JS 双花括号转义 bug：`genCells`/`aggRangeGen` 误用 `{{{`→编译后括号不匹配，node --check 发现。
  3. `aggRangeGen` 改为按显式 key 列表聚合（月模式直接用月 key，避免 formal-acc/labor-acc 显示 0/0/0）。
  4. 月模式对 day-indexed 数据（劳务工）回退按该月 day keys 求和。
  5. `toggleGenRows` 首点即展开（原首点折叠）。
  6. 体积优化：formal-att 按天仅保留大区/区域两级（仓/组 仅月模式），labor-acc 复用 attendPayload 不重复内嵌 → 16.64MB（<24MB 限制）。
- 校验：node --check 双脚本块通过；无头 Chrome + title 信标探针 0 运行时错误，6 板块全部正确渲染，下钻 expandedSubs=14。
- 发布：git push main→sprint/default（a094976..63c9963，fast-forward），release 7683362575166344398 已 finished。
- 线上链接：https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf （app_17cg3t4mvjm）
- 本地产物：补签流程_补签统计.html（16.64MB）、补签流程_补签统计.xlsx（609KB）

## 导航层级 UI 调整（同日追加）

- 需求：顶部导航从「人员类型 | 板块」改为「板块 | 人员类型」；板块作为一级切换（考勤准确率 / 考勤确认及时性 / 补签情况），人员类型作为二级切换（正式工 / 劳务工）；保留按天/按月/日期/架构控件。
- 改动（gen_buqian_stats.py）：仅调整 `top-nav-tabs` 内按钮顺序及 CSS 注释，无数据/逻辑变更。
- 校验：node --check 通过，HTML 体积仍为 16.64MB，nav 顺序确认 [secAcc/secAtt/secSign | wtFormal/wtLabor]。
- 发布：git push main→sprint/default（63c9963..8e32689，fast-forward），release 7683368593010887896 已 finished；线上链接不变。

## 导航按钮与架构控件统一（同日追加）

- 需求：一级导航「补签情况」改名为「补签率」；考勤准确率/考勤确认及时性/补签率 下的日期控件和架构控件与补签率板块截图保持一致（即增加 FBU交付前台 架构下拉）。
- 改动（gen_buqian_stats.py）：
  1. `<title>`、顶部导航按钮、页面 H1：`补签情况统计` → `补签率统计` / `补签率`。
  2. `#viewGen` 控件区新增 `#genBtnRegion` + `#genRegionDropdown` + `#genRpGrid`，与 `#viewFormal` 保持同一视觉结构。
  3. JS 新增 `renderGenRegionPanel`/`goGenMajor`/`goGenRegion`/`expandGenMajor`/`closeGenRegionDropdown`/`toggleGenRegionDropdown`，基于 `genCurrentAgg` 渲染架构快跳面板，点击大区/区域自动展开并高亮滚动到对应行。
  4. 顺手补齐通用日期下拉的外部点击关闭：`closeGenDropdown` + `genDropdown` stopPropagation + 文档 click 统一关闭所有下拉。
- 校验：node --check 通过；无头 Chrome 探针 0 运行时错误，navSign=补签率，genRegionBtn=FBU交付前台，genRegionOpen=True，genRegionGridLen=4313，6 板块全部正确渲染。
- 发布：git push main→sprint/default（8e32689..fd89ebf，fast-forward），release 7683412703169498338 已 finished；线上链接不变。

## 考勤准确率/及时性板块结构对齐补签率（同日追加）

- 需求：考勤确认及时性（正式工/劳务工）参考补签率列表结构，把原来堆叠的四张表改成「大区汇总 / 区域明细 / 仓明细 / 组明细 / Top10 员工」Tab 导航，Tab 放在卡片上方，保留按天/按月/日期/架构控件；顺便把考勤准确率板块也按同一结构统一。
- 数据改动（gen_buqian_stats.py）：
  1. 正式工 `_build_formal_recs` 额外采集姓名/工号；新增 `_build_formal_top` 按区域取 Top10（准确率按异常数，及时性按未及时确认数）。
  2. 劳务工 `build_attend_payload_v2` 新增按漏打卡数（ldk）排序的 `topEmpIndexLdk`，供劳务工考勤准确率 Top10 使用。
  3. `formalAccPayload` / `formalAttPayload` / `laborAccPayload` 分别注入 `topEmpIndex`、`topMetric`、`topMetricLabel`、`topCaption`。
- UI/JS 改动（gen_buqian_stats.py）：
  1. `#viewGen` 新增 `#genSectionTabs`（5 个 Tab），并把 4 张表分别包进 `genSecMajor/Detail/Wh/Grp`，新增 `genSecTop` + `genTableTop`。
  2. `#viewAttend` 的 Tab 移到卡片上方，「Top员工」改名为「Top10 员工」，区域/仓/组 caption 与补签率口径统一。
  3. 新增 `renderGenTop10`、`setGenMajorStateT`/`setGenRegionStateT`/`toggleGenMajorT`/`toggleGenRegionT`，实现 Top10 大区/区域展开收起。
  4. 新增通用 Tab 滚动与 scroll-spy（`scrollToGenSection` / `updateActiveGenTab`），与补签率板块行为一致。
  5. CSS 增加 `#genTableTop` 列宽与 `top-emp-row` 截断样式。
- 校验：node --check 双脚本块通过；无头 Chrome 探针 0 运行时错误。
  - genTabsText：大区汇总 区域明细 仓明细 组明细 Top10 员工
  - genTopRowsFormalAtt=154，genTopRowsFormalAcc=181，genTopRowsLaborAcc=76，attTopRows=178。
  - 6 板块全部正确渲染，架构快跳仍可用。
- 发布：git push main→sprint/default（fd89ebf..1f891bd，fast-forward），release 7683423633466232048 已 finished；线上链接不变。

## 正式工考勤准确率改为「申诉口径」（同日追加 · 与补签率报表合并）

- 需求（用户纠偏）：考勤准确率不单独出表，并入 `补签流程_补签统计.html` 的正式工维度；口径改为 **申诉人头数 ÷ 区域在职正式工总数**。源：考勤申诉流程（6月-7月）.xlsx + 在职花名册-6/-7。
- 改动（gen_buqian_stats.py）：① 删除 FORMAL_NOTE 中"准确率=正常÷全部"旧口径说明；② 将 `formal_acc_payload`（原考勤记录口径）整体重写为「申诉口径」管线：新增 `APPEAL_FILE`/`ROSTER_FILES`/`ROSTER_SHEETS` 常量、`_appeal_month`/`_roster_formal_nodes`/`_appeal_recs_all`/`_mk_appeal_rec`/`_build_appeal_acc`，复用 `_agg_formal` 聚合；③ `formal_att_payload`（及时性）与 `labor_acc_payload`（劳务工准确率）保持不变。
- 关键修正（人头去重）：`_build_appeal_acc` 先按全体在职正式工节点建 total=1 行（分母=1794/1791），再对申诉记录累计 abnormal=1；Top10 按申诉**记录数**排名，但**数据分子**按工号去重（每月每工号仅计一次），否则一人多次申诉会被重复计数 → 首版探针曾显示 6月美洲区14（应为9）。
- 结果（去重后）：6月 申诉10人/1794=0.56%（欧洲区0/美洲区9/亚太区1）；7月 申诉12人/1791=0.67%（欧洲区1/美洲区11/亚太区0）。未申诉准确率=99.44%/99.33%。
- 区域映射沿用「划分区域→REGION_TO_MAJOR」（归属大区字段不可靠）；加拿大具体区（多伦多/温哥华）报告仅映射"加拿大区"故落"其他"，本期无加拿大申诉人，分子无影响。
- 剔除申诉工号（不在当月在职正式工名册）：6月 zt0021237/zt0022134；7月 zt0019443/zt0022041(管培生)/zt0022103/zt0022335/zt0022411。
- 注：用户称"考勤准确率"实为**申诉占比**；report 同时展示"未申诉准确率=100%−占比"，并在口径说明里点明。
- 校验/发布：HTML 体积 17.45MB（<24MB）；无头 Chrome 探针（base64 beacon，捕获无 JS 错误 + 大区汇总表）确认两月口径正确——6月 申诉10/1794=0.56%（欧洲区0/美洲区10/亚太区0/其他0），7月 申诉12/1791=0.67%（欧洲区1/美洲区11/亚太区0/其他0）；去重生效（美洲区已非首版 bug 的 14）。
- 发布：copy→repo_1787290450/index.html；`git ls-remote` 确认远程 sprint/default=本地 main(1f891bd) 可 fast-forward；`git -c credential.helper= push <内嵌凭证URL> main:refs/heads/sprint/default` 成功 1f891bd..9212239；`+release-create --branch sprint/default`(7683844317043100885)→finished 无 error_logs；线上链接不变 https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf。

## 正式工考勤准确率 Top10 仅展示申诉次数>3（追加）

- 需求：用户要求「申诉次数大于3的才展示到 top10 里面」。
- 改动（gen_buqian_stats.py）：`_build_formal_top(recs, metric, min_val=0)` 新增阈值参数（默认 0 保持其他 Top10 原有行为），`lst = [x for x in lst if x[metric] > min_val]`；`_build_appeal_acc` 调用改为 `_build_formal_top(all_recs, 'abnormal', min_val=3)`；同步更新 `topCaption` 与 APPEAL_NOTE 文案注明"仅展示申诉次数>3 的员工"。
- 分布核查：整体（两月合计，仅在职正式工）按申诉记录数统计，仅 **zt0022132（GOMEZ ALCANTARA ISABEL，墨西哥3号仓/包装组）= 7 次** 超过 3；次高 zt0017649=3，故其余均被过滤。Top10 实际只剩 1 行。
- 校验/发布：HTML 17.45MB；无头探针 0 错误，Top10 仅显示 zt0022132（申诉数7，两月均列于美洲区/墨西哥区）；大区汇总不受影响（6月10/1794、7月12/1791）。push 9212239..36cb24a，release 7683851293904260375→finished；线上链接不变。

## 正式工考勤准确率口径翻转：未申诉人数÷总人数（追加）

- 需求：用户要求「考勤准确率拿未申诉人数除以总人数」（即上一轮提到的"未申诉准确率"）。
- 改动（gen_buqian_stats.py）：`formal_acc_payload` 的 pct 列 `num` 由 `'abnormal'` 改为 `'normal'`（den 仍为 `'total'`）；同步改 `subtitle` 与 APPEAL_NOTE 口径文案。数据层 `normal = total − abnormal` 不变，无需重新计算。
- 结果（无头探针 0 错误）：6月 整体 考勤准确率=(1794−10)/1794=**99.44%**（美洲区 766/776=98.7%）；7月 整体=(1791−12)/1791=**99.33%**（美洲区 730/741=98.5%、欧洲区 946/947=99.9%）。申诉占比=100%−该值（6月0.56%/7月0.67%）。
- 发布：push 36cb24a..0d0376a，release 7683860574098803927→finished；线上链接不变 https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf。

## 劳务工考勤准确率改为「账单口径」（追加）

- 需求：劳务工考勤准确率改用 `劳务工-6月份/7月份-美洲及时性.xlsx`；**总人数=账单人数，不准确人数=员工非固明细「备注」属考勤工时问题（missing hour / 考勤差异 等）的人头，考勤准确率=准确人数÷总人数**。
- 数据源结构：两个 sheet —— `员工费用核算`（**前两行均为表头**：行1分组表头、行2真实表头，数据从第3行起；列含 员工工号/区域/物理仓/组/员工）、`员工非固明细`（1 行表头，末列「备注」）。
- ⚠️ **openpyxl 坑（已解决）**：7月文件 `read_only=True` 时 dimension 异常（max_row=1，看似空表，实为 100003 行）。必须 `ws.reset_dimensions()` 才能流式读完；用 `read_only=False` 会连带解析 10 万行 sheet 而超时被 SIGTERM。
- 关键词规则（`_labor_remark_is_issue`）：`备注` 小写含 `missing` 且含 (hour|hr|rg|ot|reg|dt) 之一 → 命中；或含「考勤+差异」/「工时差异」→ 命中。该规则可自动排除 `Missing April performance Bonus`（无工时 token）与「工时正确但账单费用显示为0」（非考勤问题）等伪命中。
- 结果：6月 账单6115人/不准确156 → **97.45%**；7月 账单5889人/不准确123 → **97.91%**。美洲区 97.4%/97.8%，亚太区（澳洲/日本/韩国）100%。
- ⚠️ 两点提醒用户：① 两文件 `员工费用核算` 均恰为 100000 数据行，疑为导出截断上限；② 文件虽名「美洲」但含澳洲/日本/韩国，**无欧洲区数据**，故该板块无欧洲区。
- 改动：`labor_acc_payload` 由复用 attendPayload（应打卡/漏打卡）改为新管线 `_build_labor_acc()`（复用 `_agg_formal`，total=1/abnormal=1 记账，normal=total−abnormal）；cols=总人数/准确人数/不准确人数/考勤准确率(pct=normal÷total)；`days=[]` 强制月模式；Top10 仍保留原 ldk（漏打卡）口径未动。
- 校验/发布：HTML 17.56MB；无头探针 showCell('labor','acc') 0 错误，标题「考勤准确率（劳务工·账单口径）」，两月大区/区域明细均正确。push 0d0376a..cc1d7ae，release 7683877367245163716→finished；线上链接不变。
