# 2026-08-20 工作日志

## 补签流程-7月 补签统计
- 数据源：`D:/Documents/Downloads/补签流程-7月.xlsx`（Sheet1，4474 行）
- 口径：审批状态 = 已完成(4139) + 审批中(8) = 4147 条；排除 已驳回/已废弃/已撤回(327条)
- 维度：大区=三级部门，区域=四级部门；加拿大区归入美洲区（与现行 HR 口径一致）
- 交付：`补签流程-7月_补签统计.xlsx`（说明/大区汇总/大区_区域明细 3 表）+ `补签流程-7月_补签统计.html`（网页预览）
- 大区排序：欧洲区2740 > 美洲区1023 > 海外销售部181 > FBU HRBP Dept.139 > 亚太区30 > FBU财务部25 > 渠道管理部9
- 脚本：`gen_buqian_stats.py`
- 2026-08-20 10:17 迭代：网页版右上角新增「按天/按月」切换 + 日期/月份下拉选择器 + 左右翻页，KPI 卡与两张表随选择动态刷新；Excel 保持全量汇总不变。
- 2026-08-20 10:35 按需求剔除三个大区（海外销售部、FBU财务部、渠道管理部），不再展示；剩余 欧洲区2740/美洲区1023/FBU HRBP Dept.139/亚太区30，合计 3932 条（基于已完成+审批中）。
- 2026-08-20 10:45 剔除「美洲支持HRBP组」（3条），FBU HRBP Dept. 139→136，总计 3929 条。
- 2026-08-20 10:50 新增第五级维度「仓」（五级部门），在 HTML 新增第三张表「区域×仓补签占比」，按 大区→区域→仓 三级分组，仓带占区域比+图例，仓按区域合计降序；Excel 新增 sheet「区域_仓明细」。
- 2026-08-20 11:00 按反馈优化仓表：过滤掉无仓明细的记录，区域_仓明细从 194 行→169 行；HTML 第三张表的大区/区域分组行改为可折叠（点击 ▸/▾ 展开/收起），默认展开；JS 语法已校验通过。
- 2026-08-20 11:14 第二张「区域明细」表的大区分组行也加可折叠（toggleMajor2/setMajorState2），与第一张大区汇总表（无子级，不折叠）、第三张仓表一致；JS 语法校验通过。
- 2026-08-20 11:18 第三张表标题改为「三、大区 × 区域 × 仓（三级部门 × 四级部门 × 五级部门）补签占比」。
- 2026-08-20 11:28 合并第二份数据源 `补签流程-6月.xlsx`（3614行，覆盖 5/31–6/30）+ 原 7月文件（4474行，6/30–7/31），脚本改为读 SRCS 列表 concat。重叠日 6-30 经自然键（工号+补签日期+补签点+原首打卡+班次）校验零重叠→无重复计数。新增排除 `LD 法务部`（6月30条）。输出文件改名去掉"-7月"→`补签流程_补签统计.xlsx/html`。总纳入 7276 条（5月2/6月3354/7月3920）；美洲区2084/欧洲区4847/亚太区54/FBU HRBP Dept.291。
- 2026-08-20 13:26 按反馈将页面标题与 HTML title 统一改为「补签情况统计」。
- 2026-08-20 13:30 修复仓表与区域合计对不上的问题：五级部门为空的记录不再过滤，统一显示为「部门直属」并纳入区域_仓明细；占区域比分母改用区域完整合计（含部门直属），确保每个区域占区域比合计=100%。区域_仓明细从 169 行→216 行，含 29 条部门直属。已抽样校验西雅图区、波兰区、英国区、休斯顿区等占比合计均为 100%。
- 2026-08-20 13:47 按反馈为网页版第二、第三张表（区域明细表、仓表）添加表头冻结（`position:sticky; top:0`），长表下滑时表头保持可见。
- 2026-08-20 13:59 修复表头冻结未生效问题：原 `table{overflow:hidden}` 会建立滚动容器，导致 sticky 基准变为表格自身而非视口。改为对 `#detailTable/#whTable` 设置 `overflow:visible`，同时给 `thead` 和 `thead th` 都加 `position:sticky;top:0;z-index:10`，确保滚动到欧洲区等位置时表头仍吸附在顶部。
- 2026-08-20 14:07 在日期导航下方新增「快速定位 · 大区 / 区域」导航栏，按当前展示的大区分栏列出下属区域；点击大区/区域自动展开折叠并平滑滚动到第二张「区域明细」表对应位置（若该区域仅在第三张仓表存在则滚动到仓表）。切换日期/月份时导航栏同步刷新。
- 2026-08-20 13:51 按反馈第三版块「大区×区域×仓」不再展示 FBU HRBP Dept. 数据；区域_仓明细从 216 行→200 行（全量），HTML 7月第三版块从 197 行→180 行。大区汇总、区域明细表仍保留 HRBP 数据。
- 2026-08-20 14:16 按反馈将「快速定位」导航栏改为「区域架构 · 大区 / 区域」可折叠块，默认展开，点击标题可收起/展开；删除点击跳转定位功能，改为纯展示当前时间范围内的区域架构。
- 2026-08-20 14:20 按反馈将区域架构块升级：每个大区独立可折叠，内部按区域展示其下属仓库标签；点击仓库标签自动展开第三张仓表对应大区/区域，并滚动高亮到该仓库行。标题改为「区域架构 · 大区 / 区域 / 仓」。
- 2026-08-20 14:25 区域架构块优化：FBU HRBP Dept. 区域不再显示「无仓明细」占位标签；仓库标签排序改为「部门直属」优先，其次按名称中首位数字升序（1,2,3…），无数字项置后。
- 2026-08-20 14:45 按反馈让 FBU HRBP Dept. 下的各 HRBP 部名称也能点击跳转：将 HRBP 数据重新纳入第三张仓表（空五级部门显示为「部门直属」），区域架构块中 HRBP 区域下会显示「部门直属」标签；同时给区域标题整体加 `onclick="goRegion"`，点击后自动展开第三表对应大区/区域并高亮滚动。
- 2026-08-20 14:55 区域架构块改为三层定向跳转：点**大区标题**→滚动高亮第一表（大区汇总，按 data-major 定位）；点**区域/HRBP 部标题**→滚动高亮第二表（大区×区域明细，按 data-major+data-region 定位，自动展开所属大区）；点**仓标签**→滚动高亮第三表（仓明细，已有）。折叠功能改为点击大区标题右侧 ▾ 图标触发（stopPropagation 不再误触跳转）。第一表 major 行、第二表 region 行已加 data-major/data-region 定位属性。
- 2026-08-20 15:00 按反馈恢复第三表排除 FBU HRBP Dept. 数据，区域架构块中 HRBP 各部下不再显示「部门直属」仓库标签；HRBP 部标题仍保留点击跳转第二表的能力。
- 2026-08-20 15:05 调整区域架构块仓库标签排序：无数字的职能部门按「行政 → 渠道 → 卡派 → 管培 → 其他」固定顺序排在数字仓之后，解决原字母序导致卡派在前、行政在后的展示问题。
- 2026-08-20 15:10 区域架构块中每个区域（四级部门）标题右侧新增折叠角标 ▾，点击可展开/收起该区域下的仓库标签；区域标题本身仍保留点击跳转第二表功能。
- 2026-08-20 15:25 部署补签报表到飞书妙搭：新建独立应用 `app_17cg3t4mvjm`（补签流程统计），仓库 `https://miaoda-git.feishu.cn/apaas4.0/-/t_vkCL6f1I/code_p6iBZnfLI6KD.git`，发布成功 online_url=`https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf`。踩坑：① Git Bash 下 git 不认 `/c/Users/...` POSIX 路径（解析成 `C:/c/Users/...` 错误），须用 `C:/Users/...` Windows 原生路径；② Bash 跨命令工作区重置，clone 出的目录下一命令消失，故 clone→commit→push→release 全流程必须单命令内完成；③ `rm -rf` 被 SAFE_DELETE 拦截，用唯一目录名规避。curl 验证确认链接默认需飞书登录（创意模式 html 应用 CLI 无法设可用范围）。
- 2026-08-20 17:05 按反馈在报表中再下钻到「组」及「组下员工」：新增第四板块「大区×区域×仓×组（三级×四级×五级×六级部门）补签占比」，组=六级部门（39 种组名如交运组/包装组/拣货组，空归「未分组」，占比=该组÷所属仓合计），每个组行可点击展开组内员工明细（姓名/工号/补签日期/班次/补签点/审批状态）；第四表支持 大区→区域→仓→组 四级可折叠。Excel 新增 `区域_仓_组明细`（633 行）与 `组下员工明细`（6985 行，含补签事由列，= sub 7276 − HRBP 291）。员工明细仅在 JSON 顶层存一份全量 `allEmp`，前端按当前月/天过滤建索引，避免逐周期重复存储（HTML 体积 3.3MB 可控）。已重新部署到妙搭。
