# 2026-08-21 工作日志

- 2026-08-21 09:50 补签报表迭代：① 第四板块「大区×区域×仓×组」员工明细子表新增「补签事由」列并置于「审批状态」之前（HTML JS 表头+单元格+colspan 6→7，Excel「组下员工明细」列顺序同步调整）；② 组维度（六级部门）空值显示由「未分组」统一改为「部门直属」（与仓维度空值口径一致）。重新生成 HTML/Excel（JS 校验通过），并部署到飞书妙搭（独立应用 app_17cg3t4mvjm，仓库 code_p6iBZnfLI6KD，按 reset --hard origin/sprint/default 对齐后推送，发布成功 online_url 不变）。链接默认仍需飞书登录。
- 2026-08-21 13:46 补签报表新增第五板块「各区补签 Top10 员工」：按区域（四级部门）聚合员工补签数（已完成+审批中），各区取补签数前 10 名员工（列：排名/姓名/工号/补签数/已完成/审批中），按大区→区域可折叠展示；JS 端 buildTopEmp 从 allEmp（已改为含 HRBP 全量）按当前月/天过滤建 Top10 索引；Excel 新增「各区补签Top10员工」sheet（262 行 / 42 个区域）。数据已与源文件交叉验证一致（新泽西区徐升8/许长虎7/唐青6…）。重新生成并部署妙搭（commit f106025，发布 finished，链接不变）。部署时 git push 一度因网络卡住 11min，停掉后在已有仓库目录 repo_1787290450 续做 fetch→reset→commit→push 24s 成功。
- 2026-08-21 13:55 第五板块「各区补签 Top10 员工」增加「主属仓」「主属组」两列：Excel/Python 端按员工在区域内补签次数最多的五级部门/六级部门取主属仓/组（并列按名称字母序），HTML/JS 端 buildTopEmp 同步统计 whCount/groupCount 并取众数，表头与渲染列改为 排名/姓名/工号/主属仓/主属组/补签数/已完成/审批中，相关 colspan 6→8。重新生成 HTML/Excel（JS 校验通过），并用已有仓库目录 repo_1787290450 快速部署妙搭（commit 3344c2f，发布 finished，online_url 不变）。
- 2026-08-21 15:46 按参考样式调整顶部筛选栏布局：将「区域架构 · 大区 / 区域 / 仓」选择器从标题下方卡片移到右上角日期选择器右侧，与日期选择器并排显示；改为点击按钮弹出下拉面板（保持原有大区/区域/仓折叠结构与跳转功能）。更新 gen_buqian_stats.py 的 HTML/CSS/JS 模板并重新生成。
- 2026-08-21 16:00 新增五大板块导航标签（KPI 卡片上方 .section-tabs）：对应一~五表（大区汇总/区域明细/仓明细/组明细/Top10员工），点击平滑滚动到对应 table，滚动时自动高亮当前所在板块；给五个 table 加 id（tableMajor/tableDetail/tableWh/tableGrp/tableTop）。更新模板并重新生成。
- 2026-08-21 16:08 将 15:46（区域架构移右侧）+ 16:00（五大板块导航）两次改动合并部署到飞书妙搭（app_17cg3t4mvjm）：复用 repo_1787290450，fetch 后发现 git 2.55 仍存 refs BUG（fetch 报 sprint/default 已到 3344c2f，但 packed-refs 引用写入失败、rev-parse 仍返回旧值 613728b）；改用 python 字节替换 .git/packed-refs 第3行把 origin/sprint/default 指向 3344c2f，随后 reset --hard 对齐、覆盖 index.html、commit f5fc6ac、push 直推 sprint/default（fast-forward 成功）、release-create 发布 finished，online_url 不变（https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf）。踩坑点：单纯 update-ref 也写不进 packed-refs，必须直接字节替换 packed-refs 文件。
- 2026-08-21 16:15 将右上角「区域架构 · 大区 / 区域 / 仓」选择器按钮改名为「FBU交付前台」。修改 gen_buqian_stats.py 模板后重新生成 HTML/Excel 并部署到飞书妙搭（commit fbc341e，release finished，online_url 不变）。
- 2026-08-21 17:32 参考用户截图将「FBU交付前台」下拉面板重新设计为三大区三列总览：蓝色标题栏（FBU 交付前台 / 全球三大区总览），下面美洲区/欧洲区/亚太区三列并排，每列顶部为「大区」标题（可点击跳转第一板块），下面垂直列出该区域下所有区域（可点击跳转第二板块）。去掉原有大区/区域/仓的折叠层级与仓标签，使架构更清爽。同步生成 HTML/Excel 并部署到飞书妙搭（commit d57d557，release finished，online_url 不变）。部署前重新获取了 git 凭证（密码已轮换）。
- 2026-08-21 17:40 根据反馈补全下拉面板：新增「FBU HRBP Dept.」第四列，标题改为「全球大区总览」；每个区域下方以标签云形式列出所属仓（蓝色标签）和组（灰色标签），点击仓跳第三板块、点击组跳第四板块，并新增 goGroup 函数处理组定位与高亮。下拉面板宽度扩展为 1000px 以容纳四列。重新生成 HTML/Excel 并部署到飞书妙搭（commit 72d0d84，release finished，online_url 不变）。
- 2026-08-21 17:52 反馈下拉面板仍显乱：保留第四列（HRBP），剔除三大区下区域对应的仓/组标签云，仅保留区域名可点击跳第二板块（四列统一为纯区域列表）。改 renderRegionPanel 去掉 whMap/grpMap 标签渲染块，重新生成 HTML/Excel 并部署妙搭（commit 62d9c64，release finished，online_url 不变）。
