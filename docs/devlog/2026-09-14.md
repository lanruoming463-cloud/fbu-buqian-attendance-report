# 2026-09-14 工作日志

## 补签流程_补签统计 报表：剔除「考勤准确率」板块
- 需求：用户要求从报表中删除「考勤准确率」板块（含 正式工·申诉口径 + 劳务工·账单口径 两块）。保留 考勤确认及时性（劳务工/正式工）与 补签率。
- 改动 `gen_buqian_stats.py`：
  - 删除申诉口径代码块（APPEAL_FILE/ROSTER_FILES/_build_appeal_acc/formal_acc_payload）与劳务工账单口径代码块（LABOR_BILL_FILES/_labor_remark_is_issue/_build_labor_acc/labor_acc_payload）及对应 json。
  - 删除导航按钮 secAcc，JS 中去除 acc 分支与 formalAccPayload/laborAccPayload 引用、laborAcc 复用行；模板去掉 `{formal_acc_json}`/`{labor_acc_json}` 替换。
  - 清理遗留注释（969/971/2215 行）。
- 重新生成 HTML（EXIT=0，正式工及时确认率 98.1%）；headless Chrome 渲染验证：genTitle→「考勤确认及时性（正式工）」、大区表 14 行、无 考勤准确率 DOM 文本。
- 重新部署妙搭：app_17cg3t4mvjm（独立应用），release 7685346774630993096 finished，online_url 不变 `https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf`。
- 临时辅助脚本（_del_acc_blocks.py/_fix_acc_js.py/_fix_acc_html.py）与 dump 已清理。

## 妙搭部署实测修正（2026-09-14）
- 本环境 git 现可通过 URL 域 `!lark-cli apps git-credential-helper --app-id <id>` 凭证助手正常认证（依赖 PATH 上的 lark-cli），无需再内嵌 U/P 到 URL。`git ls-remote`/`clone`/`push` 均成功（"User cancelled dialog" 仅为系统 GCM fallback 噪音，非致命）。
- `git clone` 正确拉到 `origin/sprint/default`（packed-refs 静默失败 BUG 本次未复现）；`reset --hard origin/sprint/default` → 覆盖 index.html → commit → push `main:refs/heads/sprint/default` 即可 fast-forward。
- 跨命令工作区重置问题本次未复现：clone 出的 `repo_20260914` 在后续多条 Bash 命令中仍存在可用（仍建议关键链同命令执行以降低风险）。
- `+git-credential-list` 新版确实不带 `--app-id`（列全部）；取 repository_url / 刷新凭证用 `+git-credential-init --app-id <id>`。

## 补签流程_补签统计 报表：修复图例 & Top10 过滤
- 需求：用户截图反馈「考勤确认及时性（正式工）」的「占比图例」列进度条无蓝色填充；Top10 员工应仅展示确认不及时次数 >3 的员工。
- 改动 `gen_buqian_stats.py`：
  - CSS `.bar span` 改为 `.bar-fill{display:block;height:100%;background:linear-gradient(90deg,#2563eb,#60a5fa);border-radius:6px}`，修复进度条填充色（原 `.bar-fill` div 无样式导致只显示灰色背景）。
  - `_formal_top_untimely = _build_formal_top(_formal_recs, 'untimely', min_val=3)`，仅保留未及时确认次数 >3 的员工。
- 重新生成 HTML（EXIT=0）；headless Chrome 验证：「占比图例」列 `<div class="bar-fill" style="width:42.9%"></div>` 已带样式；Top10 共渲染 16 名员工，最小未及时确认次数为 4。
- 重新部署妙搭：app_17cg3t4mvjm，release `7685351277009603880` finished，online_url 不变 `https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf`。

## 补签流程_补签统计 报表：图例口径改为及时确认率
- 需求：用户截图反馈「考勤确认及时性（正式工）」的「占比图例」列数值/口径不对，应改为 `(总数 - 未及时确认) / 总数`，即及时确认率。
- 改动 `gen_buqian_stats.py`：
  - `renderGen` 表头：formalAtt 板块下图例列标题改为「及时确认率图例」，其他板块仍用「占比图例」。
  - `genCells`：formalAtt 板块下进度条宽度与数字改为 `(r.total - r.untimely) / r.total * 100`（及时确认率）；其他板块保持原考勤总数占比逻辑。
- 重新生成 HTML（EXIT=0）；headless Chrome 验证：表头为「及时确认率图例」，美洲区进度条宽度=98.5%、数字=98.5%，与「及时确认率」列一致。
- 重新部署妙搭：app_17cg3t4mvjm，release `7685355721801993394` finished，online_url 不变 `https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf`。

## 补签流程_补签统计 报表：异常数列→已确认数
- 需求：用户要求把「考勤确认及时性（正式工）」表中的「异常数」列替换为「已确认数」，已确认数 = 总数 − 未确认数（即 total − untimely，等价于已有 timely 字段）。
- 改动 `gen_buqian_stats.py`：`formal_att_payload` 的 `aggKeys` 去掉 `abnormal`、加 `timely`；`cols` 第 2 列由 `{'key':'abnormal','label':'异常数'}` 改为 `{'key':'timely','label':'已确认数'}`。数据层 `_agg_formal` 后已对每层计算 `timely = total - untimely`，无需额外改动。
- 重新生成 HTML（EXIT=0）；headless 验证：表头为「考勤总数 / 已确认数 / 未及时确认 / 及时确认率 / 及时确认率图例」，美洲区 已确认数=21,114（=21,437−323）。
- 部署时复现 packed-refs BUG：`origin/sprint/default` 本地引用停在旧 sha（bd92cdf），`reset --hard` 误回旧版导致 push non-fast-forward。已用 python 字节替换 `.git/packed-refs` 把引用修正为真实远端 sha（76719f5），reset→commit→push 成功（76719f5..30c0991）。
- 重新部署妙搭：app_17cg3t4mvjm，release `7685358502458690825` finished，online_url 不变 `https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf`。
