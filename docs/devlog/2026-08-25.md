# 2026-08-25 工作日志

- 2026-08-25 10:46 用户反馈五大板块表头仍未冻结（截图显示滚动后表头不可见）。CSS `position:sticky` 在飞书 WebView/本地预览环境中实际不生效，改为 JS 兜底方案：每次 `render()` 后克隆五大主表 `#tableMajor/#detailTable/#whTable/#grpTable/#topTable` 的 `thead`，创建 `position:fixed;top:0` 的克隆表头容器；滚动时检测原表位置，当表头滚出视口上方且表格仍在视口内时显示克隆表头，并同步原表列宽。生成 HTML/Excel，JS 语法校验通过，部署到飞书妙搭（commit e908260，release finished，online_url 不变）。
- 2026-08-25 11:00 用户要求 Top10 员工仅展示补签次数>3次的员工，并反馈 Top10 表格仍错行。修复：JS 端 `buildTopEmp` 增加 `.filter(o => o.count > 3)`；空职位显示为 `—`；全局 `td` 加 `vertical-align:middle`；Top10 的 职位/主属仓/主属组 列加 `white-space:nowrap` 防止行高不一致。重新生成 HTML/Excel 并部署到飞书妙搭（commit 2fcee45，release 7677799759387249962 finished，online_url 不变）。
- 2026-08-25 11:12 用户反馈 Top10 仍有错位且第二板块表头未冻结。修复：Top10 所有 `td` 强制 `white-space:nowrap!important`；表头冻结兜底增加 `document` 滚动监听 + `setInterval(updateStickyHeaders, 150)` 轮询，以应对飞书妙搭等 iframe/容器滚动场景。重新生成并部署（commit a61b196，release 7677802469233102064 finished，online_url 不变）。
- 2026-08-25 11:19 用户再次反馈 Top10 错位、第二板块表头未冻结。彻底修复：① `esc()` 对「部门直属」返回 `<span style="white-space:nowrap">部门直属</span>`，且 Top10 的职位/主属仓/主属组列加内联 `style="white-space:nowrap"`；CSS 增加 `word-break:keep-all` 和 `text-overflow:ellipsis`。② 表头冻结改用 `IntersectionObserver` 检测 thead 可见性；克隆表头挂载到 `document.documentElement` 避免父容器 transform 影响 fixed 定位；自动发现页面所有 `overflow:auto/scroll` 容器并监听滚动；`setInterval` 缩短到 80ms。重新生成 HTML/Excel，JS 语法校验通过，部署到飞书妙搭（commit cb92a10，release 7677805402277842190 finished，online_url 不变）。
- 2026-08-25 11:29 用户反馈依旧没有任何改善（仍错行、仍不冻结）。深入排查发现沙箱 git 因全局 URL-specific credential helper 配置覆盖 URL 内嵌凭证，导致近期多次 push 实际未更新远程 `sprint/default`（远程仍停留在旧 commit），release 也未真正生效。修复：push 时加 `-c credential.helper=` 强制使用 URL 内嵌凭证，确认远程 `sprint/default` 前进到 105b014。同时做代码大改：五大板块每个 table 拆分为独立 `sticky-header-table`（仅 thead）+ `data-table`（仅 tbody），caption 移为 `.section-caption`；Top10 的 `#tableTop` 加 `table-layout:fixed` 与明确列宽，避免不同区域行混排导致列宽重算、视觉上错行。重新生成 HTML/Excel，JS 语法校验通过，部署到飞书妙搭（commit 105b014，release 7677842761815166158 finished，online_url 不变）。
- 2026-08-25 13:57 用户反馈拆分 thead/tbody 后表格布局完全错乱（第一列被压窄、行重叠等）。回退到单表结构：每个板块使用 `<div class="table-wrapper">` 包裹单张 `<table>`；wrapper 使用 `clip-path:inset(0 round 12px)` 实现圆角而不像 `overflow:hidden` 那样裁剪 `position:sticky` 表头；`thead th` 仍使用 `position:sticky;top:0`。JS 兜底方案同步简化为监听单表 `thead` 并克隆为 `position:fixed`。保留 Top10 补签次数>3 过滤、职位/主属仓/主属组 `white-space:nowrap` 等改进。重新生成 HTML/Excel，JS 语法校验通过，部署到飞书妙搭（commit 904fc5f，release 7677846446133316817 finished，online_url 不变）。
- 2026-08-25 16:34 用户反馈第四板块「部门直属」出现 data-expanded/onclick 等 HTML 属性泄露为文本的乱码（截图）。根因：`esc()` 对「部门直属」返回了 `<span>` HTML，又被用在 `data-group="..."` 属性值中导致属性解析错乱、标签名泄露成可见文本。彻底修复：① `esc()` 恢复纯文本转义（移除 span 注入），新增 `attrEsc()` 专用于 `data-*` 属性；② 所有 `data-major/data-region/data-wh/data-group` 属性改用 `attrEsc`，可见文本仍用 `esc`；③ 区域面板 `rd-col-title/rd-region-title` 的 `data-*` 也统一改为 `attrEsc`。全面检查确认：五大板块 table ID 一致、表头冻结 JS 与各表 thead 对应、`.table-wrapper` 用 clip-path 不裁剪 sticky、JS 语法校验通过。重新生成 HTML/Excel，部署到飞书妙搭（commit dbb1c96，release 7677885902204030177 finished，online_url 不变）。
- 2026-08-25 16:43 用户反馈 Top10 员工列表「职位」列全部显示「—」（截图）。根因：JS 的 `buildTopEmp` 构建员工对象时遗漏了 `zhiwei` 字段。修复：在对象映射中加入 `zhiwei: o.zhiwei`，使表格已存在的「职位」列正确展示花名册匹配到的职位。重新生成 HTML/Excel，部署到飞书妙搭（commit 015c056，release 7677888349625257166 finished，online_url 不变）。
- 2026-08-25 16:51 用户反馈 Top10 表格「职位」列文字溢出到相邻单元格，出现重影/叠字（截图）。根因：Top10 固定列宽的 CSS 中，`white-space:nowrap / overflow:hidden / text-overflow:ellipsis` 的选择器误写为 `#topTable`，而实际表 ID 是 `#tableTop`，导致截断规则未生效。修复：将选择器改为 `#tableTop .top-emp-row td`，使超长职位/仓/组文字溢出时显示省略号而不叠到旁边单元格。重新生成 HTML/Excel，部署到飞书妙搭（commit 4c91bd8，release 7677890275393588438 finished，online_url 不变）。
- 2026-08-25 19:13 用户要求第四板块组下员工明细增加「备注」列，放在「补签事由」之后。修改：Excel「组下员工明细」sheet 与网页版展开子表均增加「备注」列；`emp_all` 与 `empX` 均选取源表「备注」字段并 fillna('')，避免 JSON 中出现 NaN；说明文字同步更新。重新生成 HTML/Excel，部署到飞书妙搭（commit f07d50d，release 7677926598327553204 finished，online_url 不变）。
- 2026-08-25 19:38 用户要求第五板块 Top10 补签数剔除两类记录：① 补签事由=「入职当天」；② 备注表明因客观情况无法打卡（自行定义：无打卡机/考勤机、机器故障/损坏、系统问题、闸机故障、消防、无工牌工卡等，支持中英关键词匹配）。实现：Python 新增 `is_top_exclude()` 函数，`sub` 增加 `top_exclude` 列（共剔除 1682/7276 条：入职当天 387 + 备注客观 1295，其中含 27 条「忘记打卡」但备注客观者一并剔除）；Excel「各区补签Top10员工」sheet 与网页版 JS `buildTopEmp` 均跳过 `top_exclude` 记录（新增 `if (e.top_exclude) return;`）。说明文字同步更新。重新生成 HTML/Excel，部署到飞书妙搭（commit f1ee266，release 7677932872350633200 finished，online_url 不变）。
- 2026-08-25 19:48 用户补充：西班牙语入职（Nuevo ingreso / ingreso）与「司机在外跑业务」也视为客观情况，可剔除。在 `is_top_exclude` 的 obj_patterns 中新增 `nuevo\s*ingreso`、`\bingreso\b`、`司机在外`、`在外跑业务` 等，剔除数由 1682 增至 1744（新增 76：Nuevo ingreso 61 + 司机在外 15）。重新生成 HTML/Excel，部署到飞书妙搭（commit d7d0efa，release 7677935125780270370 finished，online_url 不变）。
