# 2026-09-17 工作日志

## 补签流程报表 — 劳务工考勤确认及时率视图移除补签口径

- 用户要求：考勤确认及时率（劳务工）视图的"补签口径"去掉。
- 改动（gen_buqian_stats.py）：
  - 副标题 ATTEND_SUBTITLE 删除「一人一天应补签数最大为 2」。
  - ATTEND_NOTE_V2（数据来源/板块说明面板）删除「打卡 / 补签口径（Top10 列）」与「应补签数口径」两条（原 line 956、959）。
  - 注意：存在两处相近文案——LABOR_NOTE（劳务工补签率视图，line 600「一个人一天的应补签数合计最大为 2…」）属另一视图，保留不动。
- 重新生成 补签流程_补签统计.html（约 18MB），HTML 已无上述口径文案（grep 0 命中）。
- 妙搭发布（app_17cg3t4mvjm，旧 path）：commit 58ffc7731 → push 92be8d4..58ffc77 → release 7686336429522685124（finished，error_logs=[]）。
- online_url 不变：https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf
- 部署脚本重建为 _deploy3.py（lark-cli 取旧 path 凭证 + clone + fetch sprint/default + reset FETCH_HEAD 避开 packed-refs BUG + 覆盖 index.html + 直推 main:sprint/default + release 创建轮询）。

## 重新生成复核 + 部署 recipe 固化

- 用户要求用最新《劳务工工时》(6月20260702 / 7月20260803) 重生成并发布。重跑 gen_buqian_stats.py（EXIT=0，HTML 17.3MB、xlsx 同步）。部署时 `git status --porcelain` 为空 → 重新生成 HTML 与已发布 commit db19ec2b2 逐字节一致 → "nothing to commit"，未新建 release。结论：源数据未变，线上报表已是最新（release 7686389718095023305）。
- 按用户同意，将妙搭部署 recipe 固化进用户级 MEMORY.md（2026-09-17 新增块）：`git init`+`fetch --depth=1` 取代 clone（避开 900s 超时）；完整 ENV 块（GIT_EXEC_PATH=mingw64/bin、GIT_CONFIG_NOSYSTEM、GIT_SSL_CAINFO、HOME 临时空目录、PATH 前置）；fetch/push 带 `-c credential.helper=`；幂等退出逻辑；旧 path 仍可用。可复用脚本指向 `_deploy3.py`。

## 正式工考勤确认及时率：剔除 "nan" 区域与财务部（用户截图反馈）

- 根因：`_build_formal_recs` 里 `str(r['四级部门'])` 把空的四级部门变成 `'nan'`（7月116 + 6月90 行，三级=美洲区/欧洲区），出现在区域下拉里；财务部（l3=FBU财务部 / l4=欧洲区财务部，每月30行）此前被算进欧洲区。
- 劳务工数据核查：两月「考勤」sheet 空区域 0 行、财务 0 行 → 无需改。
- 修复（gen_buqian_stats.py `_build_formal_recs`）：三/四级部门用 pd.notna 保护；剔除 l3/l4 含「财务部」的行；剔除四级为空无法归属区域的行；打印剔除计数。FORMAL_NOTE 维度说明同步更新（范围仅美洲/欧洲/亚太+各区HRBP部；财务部整部剔除；四级为空剔除）。
- 结果：剔除 财务部60 + 四级为空206 = 266 行，考勤总数 98055→97789，及时确认率维持 98.1%；HTML grep 验证无 'nan'/财务部区域条目。
- 发布：commit 650722d（db19ec2..650722d）→ release 7686399549505588415（finished，error_logs=[]），online_url 不变。

## 补签率视图占比图例条形图修复（2026-09-17 下午）

- 用户截图反馈：补签率（正式工+劳务工）各汇总表的「占比图例」列只剩灰色空槽、无蓝色填充条。
- 根因：该视图 JS 渲染的是 <div class='bar'><span style='width:X%'></span></div>，但 CSS 只有 .bar-fill 有填充样式，span 无任何规则 → 空槽。及时率视图用 class='bar-fill' 所以正常。
- 修复：CSS 新增一条 .bar>span{display:block;height:100%;background:linear-gradient(90deg,#2563eb,#60a5fa);border-radius:6px;min-width:2px}，一处覆盖大区汇总/区域明细/仓/组 4 张表（正式工+劳务工共用模板）。
- 重新生成 + 部署：commit 66f3f02db → release 7686401422640516306（finished，error_logs=[]），数据不变（97789 / 98.1%）。
- 经验：改共享 CSS 模板时，注意同一 HTML 内并存两套 bar 渲染（.bar>span 与 .bar-fill），增删类名时两边都要查。

## 及时率视图：数据说明可折叠 + 区域排序对齐补签率（2026-09-17 傍晚）

- 需求1：考勤确认及时率（正式工/劳务工）底部「数据说明」面板可折叠。实现：genNoteBody/attendNoteBody 外加 note-header（i 图标 + 数据说明 + ▾），onclick=toggleNoteById(this)（新通用函数，按 data-notebody 定位 body），CSS 补 .note-body.collapsed{display:none}。与补签率视图的 toggleNote（写死 #noteHeader/首个 .note-body）并存。
- 需求2：两个及时率视图的架构下拉区域排序对齐补签率的固定 REGION_ORDER（美洲:新泽西/加州/亚特兰大/芝加哥/萨凡纳/达拉斯/迈阿密/休斯顿/西雅图/诺福克/印第安纳/加拿大/墨西哥/行政部/交付管理部；欧洲:英/德/捷/法/波/西/意/行政/交付；亚太:澳/日/韩；HRBP 全列表）。
  - 实现：Python 给 formalAttPayload 与 attend v2 payload 各加 regionOrder=REGION_ORDER；JS 加 roIdx/cmpRegionOrd 全局助手（主脚本模板块，双花括号转义）。
  - 改 4 处排序：renderGenRegionPanel（正式工下拉）、toggleAttendRegionDropdown（劳务工下拉，原字母序）、gen 表格 details/whs/grps（原仅按大区+区域字符串序）、attend 表格 details/whs/grps（原按 yqr 降序，现区域序为主、yqr 为同序次级）。
  - 坑：formalAttPayload 是 const 声明，不挂 window，跨函数引用需直接用标识符（配 typeof 保护）。
- 部署：commit 2fde0ba35 → release 7686405497201085714（finished，error_logs=[]），数据不变（97789 / 98.1%）。

## 及时率视图表格结构对齐补签率：分组折叠改造（2026-09-17 晚）

- 需求：考勤确认及时率（正式工/劳务工）下几个模块的展示形式与补签率保持一致——上面是大区/区域组头、下面是缩进的仓/组行，不要「维度」列斜杠拼接（如「美洲区 / 新泽西 / 仓 / 组」）的平铺长表。
- 实现（renderGen + renderAttend 各四张表）：
  - 板块一大区汇总：平铺，行=大区（去掉行内「（大区合计 x）」尾巴）。
  - 板块二区域：每大区前插 .grp 组头行「▾ 大区（大区合计 x）」，区域为缩进子行（td.indent），占比基准=大区合计（原为总合计）。
  - 板块三仓：大区组头 + .subgrp 区域组头「▾ 区域（区域合计 x）」+ 仓子行（td.indent2），占比基准=区域合计。
  - 板块四组：大区/区域/.subgrp3 仓三级组头 + 组子行（td.indent3），占比基准=仓合计（原为总合计）；劳务工组行保留员工展开。
  - 表头从「维度/大区 / 区域」改为「大区」「区域（四级部门）」「仓（五级部门）」「组（六级部门）」，与补签率一致。
  - 事件处理重写：gen 视图 ghd-m/ghd-r/ghd-w 组头（genHid/genAnyHid/genHIcon），att 视图 ahd-m/ahd-r/ahd-w（attHid 等）；点大区组头级联折叠下级，子级保持各自折叠状态。
  - 修 goGenRegion：架构面板点区域直接定位 #genDetailBody 的区域行（新结构所有行都渲染、无 att-sub）。
- 顺带修存量 bug：esc()/attrEsc() 对数字崩溃（(5||'').replace is not a function），劳务工组展开员工必现（员工明细含数值列），探针 attEmpRows=0→1。改为 typeof s!==string 时 String(s)。
- 验证：静态 grep（ghd-*/ahd-* 组头数、旧斜杠拼接 0 处、双花括号 0）+ 无头 Chrome 探针 _probe18.py（正式工 4 大区/40 区域/226 仓/759 组，劳务工 3 大区/20 区域/139 仓/637 组，折叠/展开/员工展开均 true，errors=[]）。
- 部署：commit ae16ea147 → release 7686415315316493512（finished，error_logs=[]），数据不变（97789 / 98.1%）。
- 坑：劳务工视图模板块是单花括号（{attend_json} replace 注入），CSS/JS 写双花括号会原样输出错误——本次 CSS 一度写成 {{}} 后已改回。

## 及时率视图页面布局加 .wrap 容器（2026-09-17 晚）

- 问题：考勤确认及时率（正式工/劳务工）页面太宽，没有 max-width 限制；补签率视图有 .wrap{max-width:1100px;margin:0 auto} 包裹，布局居中且阅读舒适。
- 根因：主模板只包裹了补签率视图（viewFormal），viewGen（正式工及时率）和 viewAttend（劳务工及时率）在 wrap 外。
- 修复：
  - viewGen 前加 `<div class="wrap">` 开始标签；
  - viewGen 的 `</script>` 后加 `</div>` 闭合；
  - viewAttend 的 `</script>` 后加 `</div>` 闭合（劳务工视图是独立 attend_block 追加到 `</body>` 前）。
- 验证：HTML 中 `.wrap` 容器数=2（补签率 + 及时率共用），viewGen/viewAttend 均在 wrap 内，max-width:1100px 规则存在。
- 部署：commit 2446b9e90 → release 7686420733858663610（finished，error_logs=[]），数据无变化。
- 效果：三视图（补签率/正式工及时率/劳务工及时率）布局宽度一致，均限制为 1100px 居中。
