# 2026-08-27 工作日志

## 补签流程统计报表（劳务工补签率）
- 2026-08-27 10:18 用户要求劳务工「补签率」= 应补签数 ÷ 应打卡数。修改 `gen_buqian_stats.py`：
  - 流式读取新增保留 `应打卡数`/`应补签数` 两列（不预过滤，保留全量考勤行）。
  - 新增 `add_labor_rate(agg, df_all)`：按 三级/四级/五级/六级部门 聚合 `应打卡数`/`应补签数` 求和，补签率 = 应补签数/应打卡数；并给每月/每日 agg 结果补充 `rateTotal/ydkTotal/ybqTotal`。
  - JS `aggRange` 改为同时累加各天的 `ydkTotal/ybqTotal` 再算区间补签率（避免按日率简单平均）。
  - 板块一~四表格均新增「补签率」列（大区/区域/仓/组），KPI 卡新增「补签率（应补签÷应打卡）」；通过 `body.labor-rate` 类控制：正式工视图隐藏该列与卡片，劳务工视图显示。
  - 修复 bug：`add_labor_rate` 中 groupby 已把列 rename 为 `ybq`，但取值误写 `v['应补签数']` → 改为 `v['ybq']`（KeyError）。
  - 校验：2026-07 劳务工 大区补签率 美洲区 5.6% / 欧洲区 28.4% / 亚太区 1.8%，整体 18.0%，与 应补签/应打卡 手工核对一致。
  - 重新生成 HTML(9.8MB)/Excel 并部署（commit ec36d6e，release 7678530274892713207 finished，online_url 不变 https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf）。
- 2026-08-27 10:24 用户反馈正式工视图表格局部多出一列「—」、占比图例条右移。根因：新增劳务工「补签率」列后，tbody 单元格 class 为 `.rate`，CSS 只隐藏表头 `.rate-col`，未隐藏 tbody `.rate`，导致正式工表格实际多出一列。修复：`gen_buqian_stats.py` 中 (1) CSS 增加 `body:not(.labor-rate) .rate {{display:none}}`；(2) JS `render()` 改为根据 `payload.type === 'labor'` 动态生成/跳过「补签率」列，动态设置表头与 colspan，正式工 DOM 中不再残留隐藏列。重新生成并部署（commit b8c6c57，release 7678534388333907140 finished，online_url 不变）。

- 2026-08-27（下午）用户要求只调整劳务工板块的口径：补签合计=应补签数，已完成=已补签数（=实际补签数），审批中改名为未完成=应补签数−已补签数。修改 `gen_buqian_stats.py`：
  - 新增 `agg_period_labor(df, major_order, region_order)`：输出结构与 `agg_period` 完全一致（保留 '已完成'/'审批中'/'total' 键），但改为求和口径——补签合计=Σ应补签数、已完成=Σ实际补签数、未完成=Σ应补签数−Σ实际补签数；pct 分母用 Σ应补签数（安全除零）。板块一~四调用改为 `agg_period_labor`。
  - 劳务工「补签记录」过滤改为 应补签数>0 或 实际补签数>0（原仅实际补签数>0）；`L_agg` 增加 `应补签数`/`实际补签数` 两列。
  - empagg/topagg 的「补签次数」由 size 计数改为 Σ应补签数 求和；topEmpIndex 的 done=Σ实际补签数、ing=补签次数−done。
  - 展示层：JS `render()` 新增 `ingLabel`（劳务工=「未完成」/正式工=「审批中」）并用于板块一~四表头与 Top10 表头（thTopIng）；KPI 卡 `cTotalL`/`cIngL` 与板块五说明 `secTopCap` 在 `switchWorkerType` 中按视图动态切换文案；LABOR_SUBTITLE/LABOR_NOTE 同步更新口径。正式工视图完全不变。
  - 校验：劳务工全期 补签合计=138275、已完成=138043、未完成=232；按月 2026-06: 61323/61202/121，2026-07: 76952/76841/111；无任何「实际>应」导致负未完成的行。重新生成 HTML/Excel 并部署（commit 2900ba2，release 7678541951414701298 finished，online_url 不变 https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf）。

- 2026-08-27 11:18 用户要求：1) 劳务工板块二区域按正式工架构顺序排序（美洲区→欧洲区→亚太区）；2) Top10 仅统计补签次数 > 3 的员工。
  - 修改 `gen_buqian_stats.py` 的 `build_labor_payload`：
    - 区域排序不再使用 `sorted()`，而是复用模块级 `REGION_ORDER`（正式工架构顺序），按实际存在的区域过滤，缺失区域排在末尾。大区顺序保持 `['美洲区','欧洲区','亚太区']`。
    - Top10 聚合后增加 `.g['补签次数'] > 3` 过滤，再 sort + head(10)。
  - 校验（2026-07）：美洲区区域顺序 = 新泽西/加州/亚特兰大/芝加哥/萨凡纳/达拉斯/迈阿密/休斯顿/西雅图/加拿大；欧洲区 = 英国/德国/捷克/法国/波兰/西班牙/意大利；亚太区 = 澳洲/日本/韩国。所有 Top10 列表最小 count=4，韩国区无满足条件员工故为空。
  - 重新生成 HTML/Excel 并部署（commit 1589e9c，release 7678551462725029062 finished）。推送时遇到 GCM 凭证助手挂起，使用 `git -c credential.helper= push` 绕过。online_url 不变 https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf。

- 2026-08-27 13:24 用户要求：仓（五级部门）明细按名称内嵌数字自然排序（1-10…），替换原按 total 降序。
  - 在 `gen_buqian_stats.py` 新增模块级 helper `wh_num(name)`：正则提取名称中第一个整数，无数字（如「部门直属」「卡派组」）返回 10^9 排末尾。
  - `agg_period`（正式工）与 `agg_period_labor`（劳务工）的仓聚合 sort 改为 `['__mo','__ro','__wn','total']`（ascending True/True/True/False），先大区→区域→数字顺序→total 降序。
  - 校验（劳务工 2026-07 新泽西区）：1,2,3,5,6,7,8,10,11,…,25 号仓自然升序；「卡派组」无数字排末尾。
  - 重新生成并部署（commit 6330c58，release 7678578993419046184 finished）。推送用 `git -c credential.helper=` 绕过 GCM 挂起。online_url 不变。

- 2026-08-27 13:32 用户要求：正式工的日期筛选和 FBU 交付前台导航栏与劳务工保持一致，放到维度下面。
  - 修改 `gen_buqian_stats.py` HTML 模板：将 `<div class="header">` 内的 `<div class="controls">` 移出 header，放到维度副标题下方、section-tabs 上方；header 改为 `display:block`。
  - CSS：`.header` 移除 `justify-content:space-between` 等 flex 属性；`.controls` 增加 `margin:12px 0 18px`，保持筛选栏在维度下方、导航栏上方。
  - 重新生成 HTML/Excel 并部署（commit 6424d73，release 7678581345681902892 finished）。online_url 不变 https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf。

- 2026-08-27 13:42 用户要求：劳务工的补签率 KPI 图例去掉。
  - 修改 `gen_buqian_stats.py`：删除 KPI 卡片 `<div class="card" id="cRateCard">...</div>`；删除 CSS `body:not(.labor-rate) #cRateCard{display:none}`；删除 JS 中 `document.getElementById('cRate').textContent = ...` 更新逻辑。
  - 表格中的「补签率」列仍保留（仅去掉顶部 KPI 卡片）。
  - 重新生成 HTML/Excel 并部署（commit 0dec272，release 7678583517824142554 finished）。online_url 不变 https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf。

- 2026-08-27 14:49 用户要求：在板块一~四「补签合计」前加一列「应打卡合计」。
  - 仅劳务工视图有「应打卡数」数据源（正式工源表无此列，故该列只加在劳务工视图）。
  - 修改 `gen_buqian_stats.py` JS `render()`：新增 `showYdk = payload.type==='labor'`；`C = (showRate?1:0)+(showYdk?1:0)+6`（劳务工=8列，正式工=6列不变）；表头与板块一~四行渲染（major/detail/wh/grp）在「补签合计」前插入 `ydkCell(r)`（=r.ydk，即 Σ应打卡数合计）。
  - 数据来自 `add_labor_rate` 已注入的 `r['ydk']`（各层级均已计算）。2026-07 校验：美洲区 ydk=169054、欧洲区=191346、亚太区=6050。
  - 重新生成 HTML/Excel 并部署（commit 0334993，release 7678601661385329851 finished）。online_url 不变 https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf。

- 2026-08-27 14:59 用户要求：1) 去掉劳务工板块说明中「补签事由（异常原因）中属客观无法打卡的，已在 Top10 中按与正式工一致的口径剔除」这一条；2) 注意一个人一天的应补签数最大为 2。
  - 修改 `gen_buqian_stats.py` 的 `LABOR_NOTE`：删除 Top10 补签事由剔除说明，替换为「应补签数口径：一个人一天的应补签数最大为 2，对应上下班两次打卡；超过 2 的情况请检查源数据是否存在重复排班或异常」。
  - 重新生成 HTML/Excel 并部署（commit e9ac25b，release 7678605579770727605 finished）。online_url 不变 https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf。

- 2026-08-27 15:16 用户要求：应补签数口径=一个人一天最大2，超过2取2；对应的数据有问题的也要更新。
  - 探查：劳务工考勤表工号+考勤日期共 39.5万组合，其中 297 个合计应补签数>2（最大6，多为同一人多行各计2）；另有1行单条=3。
  - 修改 `gen_buqian_stats.py`：在 L 数值转换后新增封顶逻辑——按 (工号,考勤日期) 对应补签数做累计，超过 2 的部分截断（new=min(应补签数, max(0, 2-前序累计))）；并同步 `实际补签数=np.minimum(实际补签数, 应补签数)` 保持 未完成>=0。需 import numpy。
  - 更新 `LABOR_NOTE` 说明：超过2已按员工+考勤日期封顶取2并同步限制实际补签数。
  - 校验：应补签数合计 138275→137678（减597）；实际补签数 138043→137447（减596）；封顶后工号+日期合计最大值=2、>2组合数=0、未完成最小值=0。
  - 重新生成 HTML/Excel 并部署（commit 44af12c，release 7678608651594747120 finished）。online_url 不变 https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf。

- 2026-08-27 15:57 用户要求：劳务工板块四点击组展开员工时，展示明细数据（姓名、工号、工种、考勤日期、班次、补签时间）。
  - 修改 `gen_buqian_stats.py`：
    - `_labor_cols` 增加「首打卡补签时间/末打卡补签时间/休息开始补签时间/休息结束补签时间」。
    - 新增 `_fmt_time()` 格式化四个时间字段为 HH:MM，合并为「补签时间」列（分号分隔同一天同一班次的多个时间点）。
    - 员工明细 `empagg` 改为按 (大区/区域/仓/组/姓名/工号/工种/考勤日期/班次) 聚合，保留行级明细；`empIndex` 键改为「工种」「考勤日期」「班次」「补签时间」。
    - 更新 `LABOR_EMP_KEYS`/`LABOR_EMP_HEADERS` = ['姓名','工号','工种','考勤日期','班次','补签时间']；更新板块四说明。
  - JS 端：`render()` 中 `C` 提升为全局变量；`toggleEmp()` 的 `colspan` 从固定 6 改为 `${{C}}`，空提示同理；展开计数单位劳务工用「条」、正式工用「人」。
  - 校验：劳务工 empIndex 共 88,397 条明细；样本显示姓名/工号/工种/考勤日期/班次/补签时间均正确；补签时间格式化为 HH:MM。
  - 重新生成 HTML/Excel 并部署（commit e89aa3e，release 7678623509832404156 finished）。online_url 不变 https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf。

- 2026-08-27 16:30 【线上修复】用户反馈飞书链接点不开，报错「应用产物文件过大」。
  - 根因：劳务工 `empIndex` 将 88,397 条员工明细用中文 key 对象全量内联，HTML 体积达 **24MB**（labor payload 14.6MB），远超妙搭单文件限制（历史 9.8MB 可过，24MB 拒）。
  - 压缩方案（保留「点击组展开明细」功能）：
    1. 员工明细从 `{中文key:值}` 对象 → **位置数组** `[姓名,工号,工种,考勤日期,班次,补签时间]`，去重中文 key；
    2. 每组明细**裁剪为前 50 条**展示（报表用，全量见 Excel「组下员工明细」），新增 `empCount` 字典记录每组真实总条数；
    3. JS `toggleEmp()` 改为兼容数组格式（按 `empHeaders` 顺序渲染），展开标注「共 X 条，显示前 N 条」。
  - 结果：HTML 24MB → **9.57MB**（labor payload 14.6MB→4.3MB，明细行 88,397→16,991），功能完整。
  - 部署复盘坑：① `lark-cli` 无扩展名在 Python 子进程找不到 → 用绝对路径 `…/node/versions/22.22.2/lark-cli.cmd` 在 Bash 直接调；② Git Bash 的 `/tmp` 与 Windows Python 解析不一致 → 用绝对 Windows 路径（如 `C:/…/_cred.tmp`）暂存凭证且同一条命令内读取并删除；③ `lark-cli … get | python` pipe 会报 "pipe is being closed" → 改 `> file` 重定向再读；④ push 用 `git -c credential.helper= push "$URL" main:refs/heads/sprint/default`，URL 内嵌凭证（用完即删）。
  - 重新生成 HTML/Excel 并部署（commit c44235f，push 后远程 sprint/default 已= c44235f；release 7678631458043874503 finished）。online_url 不变 https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf。

- 2026-08-27 17:00 用户反馈：① 劳务工组下员工明细的「补签时间」需拆分为「首打卡补签时间」「末打卡补签时间」，不展示休息补签时间；② Top10 的「职位」改名为「工种」。
  - 修改 `gen_buqian_stats.py`：
    - 删除 `L['补签时间']` 合并列；员工明细 `empagg` 改为分别聚合 `首打卡补签时间`/`末打卡补签时间`。
    - `LABOR_EMP_KEYS`/`LABOR_EMP_HEADERS` 更新为 ['姓名','工号','工种','考勤日期','班次','首打卡补签时间','末打卡补签时间']；`empIndex` 位置数组同步改为 7 列。
    - 劳务工 Top10 数据字段 `zhiwei` 改为 `gongzhong`；JS 端 `#thTopZhiwei` 表头根据 `payload.type` 动态显示「工种」/「职位」，行渲染也取对应字段。
    - 更新板块四说明文字。
  - 校验：empIndex 首行示例 `['ANDRII MAZUR','POL076177','操作员','2026-07-31','...','18:00','04:30']`，首末打卡分离正确；HTML 体积 9.57MB → **7.7MB**（进一步缩小）。
  - 重新生成 HTML/Excel 并部署（commit 390c768，release 7678637729340689673 finished）。online_url 不变 https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf。
