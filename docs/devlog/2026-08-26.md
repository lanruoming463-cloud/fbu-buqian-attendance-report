# 2026-08-26 工作日志

## 补签流程统计报表
- 2026-08-26 09:33 用户反馈底部「说明：」文字排版不够美观（截图）。重新设计说明板块：改为浅蓝色信息卡片样式，带「i」图标与「数据说明」标题；内部分为「数据来源 / 交互方式 / 口径定义 / 第四板块 / 第五板块」五个分区，每区有蓝色标签；条目使用统一圆点列表，关键术语用 `<code>` 和 `<strong>` 高亮；整体加渐变背景、左侧蓝色氛围边框、分区虚线分隔。更新 `gen_buqian_stats.py` 的 `.note` CSS 与 HTML 结构，重新生成 HTML/Excel，部署到飞书妙搭（commit 0879a1c，release 7678147696456437022 finished，online_url 不变）。
- 2026-08-26 10:10 用户要求「数据说明」板块可折叠。实现：`.note-header` 改为可点击（`onclick="toggleNote()"`），右侧加「▾」折叠角标，点击切换 `.collapsed` 类隐藏/显示 `.note-body`（CSS 加过渡动画）；JS 新增 `toggleNote()`。部署时又踩 git 2.5x packed-refs BUG：fetch 报告 sprint/default 前进到 0879a1c，但本地 ref 实际仍是旧的 f1ee266，导致首轮 reset 落到错误基底（commit 4c3e38a 挂在 f1ee266 上）。用 python 字节替换 `.git/packed-refs` 将 sprint/default 修回 0879a1c 后，`reset --hard` 重基于 0879a1c 重新 commit（4405fda），push 成功为 fast-forward。重新生成 HTML/Excel，部署到飞书妙搭（commit 4405fda，release 7678157664741002530 finished，online_url 不变）。
- 2026-08-26 14:34 用户要求顶部增加类似「FBU 交付前台 · 全球」的导航栏，区分「正式工 / 劳务工」视图（截图）。实现：在 `.wrap` 顶部新增 `.top-nav`（左侧 globe 图标+标题「FBU 交付前台 · 补签」+副标题，右侧两个标签页按钮）；将原有报表内容包入 `#viewFormal`，新增 `#viewDispatch` 占位面板，点击「劳务工」显示占位提示「请将劳务工补签数据源路径发给我...」；JS 新增 `switchWorkerType()`，切换时同步处理表头冻结的创建/销毁。重新生成 HTML/Excel，部署时再次用 python 修复 git 2.5x packed-refs（0879a1c→4405fda）后成功 fast-forward 部署（commit e3236a6，release 7678226590489054393 finished，online_url 不变）。劳务工数据源待用户提供。
- 2026-08-26 16:47 用户提供劳务工数据源（劳务工补签-6月/7月.xlsx），正式构建劳务工版补签统计（参考正式工、同五板块：大区汇总/区域明细/仓明细/组明细/Top10员工）。劳务工无「补签申请」概念，定义「补签记录」= 实际补签数>0 的每日考勤行；大区由「劳务工架构」sheet 区域→大区映射得到。`build_labor_payload()` 复用 `agg_period`（传 labor 专属 major/region 排序）。踩坑与修复：(1) OOM——`pd.read_excel` 全量读 214K×55 行撑爆沙箱内存，改用 openpyxl `read_only` 流式逐行读取+仅留11列+过滤，并在构建劳务工前 `del` 正式工 `pdf/sub` 等大对象；(2) `KeyError: '三级部门'`——`L` 仅保留 `大区` 列，empagg/topagg 按 `三级部门` 分组报错，改为在 `L` 上直接 rename `大区→三级部门`；(3) JS 切换后数据串台——`const data = payload.data` 在加载时固定捕获正式工数据，切到劳务工仍读旧 `data`，改为 `let data` 并在 `switchWorkerType` 中 `data = payload.data` 重绑。校验：劳务工 months ['2026-06','2026-07']，2026-07 共 47915 条补签、4 大区/19 区域/134 仓/574 组；empIndex 620 组、topEmpIndex 21 区域各 Top10。重新生成 HTML(9.2MB)/Excel 并部署（commit 34267a8，release 7678261094872386783 finished，online_url 不变 https://ztn.larkenterprise.com/page/RgGYm9pJkdaDGKafK8lc5pS1ntf）。注意：创意模式 html 应用链接默认需飞书登录，公开需妙搭后台配可用范围。
- 2026-08-26 17:19 用户反馈劳务工视图出现「其他」大区，要求按区域名显式映射：欧洲区=英国/德国/捷克/法国/波兰/西班牙/意大利；亚太区=澳洲/日本/韩国；其余全部归入美洲区。修改 `build_labor_payload()`：弃用「劳务工架构」sheet 映射，改用显式规则函数 `region_to_major()`（兼容「英国区/英国」两种写法），并基于数据重新计算 major/region 排序。修复后发现区域名实际以「区」结尾（如英国区、德国区），又补一次兼容处理（去尾「区」再匹配）。重新生成 HTML/Excel 并部署（commit 8b9ae54，release 7678272129934855362 finished）。2026-07 劳务工大区分布：美洲区 8002(16.7%)、欧洲区 39798(83.1%)、亚太区 115(0.2%)，已无「其他」。
